name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: cargo install just && just env && just package-build
    - name: Run tests
      run: cargo install just && just env && just package-test
